apiVersion: v1
kind: Service
metadata:
  name: cert-server-service
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: cert-server
  ports:
    - port: 8080
      targetPort: 8080
      name: http
