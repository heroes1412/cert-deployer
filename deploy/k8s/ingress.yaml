apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cert-vault-server-ingress
  namespace: default
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  rules:
    - host: cert-vault.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cert-vault-server-service
                port:
                  number: 8080
  tls:
    - hosts:
        - cert-vault.example.com
      secretName: cert-vault-tls-secret
